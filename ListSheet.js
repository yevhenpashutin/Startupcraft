import React from "react";
import { StyleSheet, Text } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingTop: 100
  }
});

const ListSheet = () => {
  return <Text style={styles.container}>ListSheet</Text>;
};

export default ListSheet;
